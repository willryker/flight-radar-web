"""
Flight radar web page -- supporting modules.

server.py is the entry point and owns the HTTP layer and the pollers. These
modules hold the parts that are worth reading on their own: the geometry, and
everything ADS-B does not carry.
"""
